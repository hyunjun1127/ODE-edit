# EP-TW-1 cap-only sweep

Authority: ODEEDIT-S06-EP-TW1-ALPHA-CAP-SWEEP-SH4-V1. CAP1 47962 is reused;
CAP10/CAP100/NORM_ONLY each start cold W0/M0 on the same first1000 B100×10.
The scientific change is only the bound of alpha. `disabled` requires JSON
null and uses alpha_norm directly, not infinity or a large finite proxy.
Finite checks, target ball, executable correction trust, exact quality and
ID-set screen, full candidate menu, history, ledger and observers are retained.

`sweep_control init/check/freeze/submit --worktree <clean-worktree>` writes
create-once receipts under the fixed local task root. Freeze requires committed
source and narrow CPU checks. Each GH arm science lock is checked individually;
no aggregate contract bypasses single-chain validation. Existing non-EP source
closure is copied from the old frozen source, not silently updated to main.
Teacher/model/P full SHA evidence is reused with current stable stat; small
assets and executable sources are rehashed. No new teacher or baseline run.

Submission registers all three chains upfront. One reserved CAKE slot leaves
one afterany sweep lane; no CAKE reservation allows two sweep lanes. Afterany
does not condition an independent arm on earlier quality or technical success.
Unrelated jobs are never changed. Each arm keeps ten W4/M4/RNG/ledger snapshots
and its terminal weight. Existing CP1 absence does not justify a reference rerun.

The original diagnostic waiver remains SKIPPED_USER_DIRECTED, numerical_validation
NOT_ESTABLISHED. CPU cap1 byte equality is not a neural derivative/GPU validation.
Unlike prior EP/CAKE tasks, this task continues after initial/PENDING until all
authorized arms and the factual report are completed. No additional arms or
automatic followup experiments are authorized.
