# ORBODE sequential cumulative rerun: claim–measurement lock

User-directed observation-only augmentation, 2026-09-06. This is not a new
arm/grid selection. Proposal read in full (1–1359); SHA256
`03a6fc61258e7643fc281fab8ab0d3ca700bb80f09aaa3eb34591ce5827087be`.
Exact authoritative source is the GH-transferred private local seal, not an
invented Git proposal copy. The accepted fast-main arm contract and its later
relaxed scientific gates supersede broader proposal promotion/first-hit plans.

## Fixed execution and inference boundary

LM/LA/QM/QA; each O,QCL,NQFIX,ORBFH,JAC; independent arm entry W0/cold state,
within-arm B1→B10 cumulative physical W/history; 100 requests per batch.
T=1,N=4,h=.25; unchanged native targets, numerical lock, family hparams,
controller, evaluator, sample/order. ORBHit remains derived, never a sixth run.
All added evaluations/probes are diagnostic-only; no value returns to the
controller, selector, writer, or success-dependent sample selection.

## Proposal claims and required evidence

| Claim/question | Matched comparison | Recorded evidence | Boundary |
|---|---|---|---|
| Removing prescribed R/n quota | QCL vs NQFIX | current A, actual Y, E=A−Y, normalized norms, rho/tau, RS/PS/NS and parameter path/net action | O is state-reactive, schedule-open-loop; do not describe it as ignoring upstream state |
| Response multiplier apportions useful progress | NQFIX vs ORBFH | g,r,a*=max(g,0)/r,u,alpha; u=0/interior/1; predicted/actual V reduction; finite-step defects; command/JVP/model error M | zero/stall/degradation remains an observation, not arm pruning |
| Realized-state rebuild within sweep | ORBFH vs JAC | build/key/state identities, same-layer revisit key and writer changes, closure/progress and cumulative retention | revisit drift is NOT a same-entry counterfactual delta-K caused by one upstream layer |
| Layer redistribution | all five arms | current command and realization; per-request old/new q and signed progress at every layer visit; sum h*u workload and signed V reduction; layer path/update shares | historical requests have no new command: label residual preservation/damage, never allocation realization |
| Action–realization efficiency | all five arms | per-request residual decrease per batch-level applied Frobenius norm; zero-action typed; layer batch net, W0-to-current net, sum step norms/squares; dynamic resolution-stable sum h*u²*||B||² | scalar reductions FP64; FROBENIUS surrogate is NOT native C_reg quadratic action; batch-level action is not 100 independent action samples |
| Fixed-z stability and tails | all five arms | each cohort original z and origin SHA; q before/after each visit, immediate-vs-later activation drift, per-request deterioration; existing q/g/r/V/defect rows | no z refresh for historical probes, no epsilon hiding zero origins; mean, median, IQR, p90/p99/max and CVaR derivable from complete rows |
| Stopping/overtracking | ORBFH vs derived ORBHit | retained existing prefix/semantic/action receipt and endpoint audit | no second trajectory or extra arm; missing derived endpoint is WITHHELD, not imputed |
| Genuine sequential retention | all five arms at W_B1…W_B10 | each frozen W evaluates every seen request, including canonical rewrite/rephrase/neighborhood new/true pairs, token/strict secondary metrics; cohort hashes and order | CHECKPOINT_W_ON_ALL_SEEN_REQUESTS, not online-at-edit aggregate; final W_B10 evaluates 1000 once |
| Historical mechanism retention | each cohort through subsequent B commits and layer visits | original fixed target/origin, immediate endpoint activation, per-layer historical q/response/potential; same checkpoint performance | association only; cross-arm targets after B1 are naturally method/state-specific |
| Recoverable final/intermediate evaluations | each arm/batch | exact five FP32 edited weights, tensor/file hashes and CPU exact reload, pinned original model binding | evaluation-only checkpoint, not editing-resume cache checkpoint |

## Aggregation and complete denominators

Per arm: 10 W checkpoints, 55 evaluated B100 cohorts = 5500 request-state rows;
rewrite 5500, rephrase 11000, neighborhood 55000 prompt pairs. Four cells ×
five arms: 200 checkpoints, 110000 request-state rows. Final W_B10 per arm:
1000 requests, rewrite1000, rephrase2000, neighborhood10000. Ties fail strict
NLL inequality: RS/PS new<true, NS true<new. Token accuracy/strict and PP-token
preservation (where recorded) are distinct secondary metrics, never NS.

Every visited layer probes all seen cohorts using their original z/origin;
current-edit A/Y/E and historical response are separated. Missing/no-op visits
are not fabricated. Per-batch and cumulative action denominators are distinct.
Dynamic per-visit action is the nominal low-rank alpha*B Frobenius norm, not
the FP32 rounding difference of stored weights; Official per-layer action and
all arms' committed batch-net/W0-to-current norms use actual stored weights.
Preserve per-request/per-layer rows for age/cohort, early/late, tails and paired
comparisons. Compare method/arm at shared request/order/checkpoint, without
equating different cohort-native z. Exact-state activation and CounterFact
evaluation use their existing, separately identified context contracts.

## Explicitly not established by this rerun

- Native C_reg action is UNBOUND in the accepted runtime. Do not relabel
  Frobenius norm/squared norm as covariance action or an optimal-control cost.
- One fixed N=4 run does not establish step-size/continuous-ODE convergence.
- No added matched-no-revisit, same-entry causal layer counterfactual, strong
  static solver, or continuation arm: those proposal claims are NOT_TESTED.
- No causal guarantee, universal architecture claim, or scientific promotion.

## Observer and resource gates

Observer on/off toy byte parity, Official call-scoped finally restoration,
all-seen order/cardinality/raw-free reducer, zero-denominator handling,
checkpoint CPU recovery and weight pointer/version/bytes/cache invariance.
Existing sequential fixed-z/cache/W-chain/FP32/source/session/offline gates
remain. Pure observation exceptions fail closed in a distinct namespace.
Cap2, array0-3%2, node server4, each1GPU/8CPU/mem60416M. Existing artifacts
immutable. Storage reserve >=128GiB; checkpoint estimate sealed before submit.
Added model forwards/evaluator time are explicitly extra diagnostic overhead,
not claimed to be zero-cost or included in controller-only speedup claims.
