# E0/E1 baseline mechanism-first diagnostics

This package binds the original BLUE singleton dense writer; it is not an
editor variant. Existing experiment sources remain read-only. Execution
authority: `ODEEDIT-S06-BASELINE-MECHANISM-FIRST-E01-SH1-V1`.

Current first wave: cold AlphaEdit BLUE L4, first fixed B100 only. The planned
20 cells and up to 155 native batches are not implemented/completed by this
first wave. Warm checkpoint verification, the general corpus panel and full
E1 instrumentation remain explicit work after USER recall. No E2–E5.

`assets.py` verifies the original execution lock against the local unchanged
native closure, model snapshot, projector and exact evaluator helper bytes.
`fixtures.py` owns rollback; `native_runner.py` invokes native dense write;
`observer.py` captures its actual K/R/RHS/update and verifies native history.
`evaluation.py` reuses the original canonical NLL-pair kernel/reducer.
`signed_response.py` performs diagnostic all-position contractions, not
activation intervention or update selection. `evidence.py`/`case_analysis.py`
are CPU-only evidence/join primitives. General text is not an editor input.

CPU checks:

```sh
/mnt/raid5/janghj/EasyEdit/.venv/bin/python -m unittest discover \
  -s project/run_scripts/baseline_mechanism_first/tests -t . -q
```

The launcher must be submitted held, inspected for source/owner/args/resources,
and released only inside prospective cap2. Input lock and output are task-local
create-once paths, not tracked raw assets. First source-exact W0/context/native
write/history plus representative signed audit yields INITIAL_VALID, not whole
E0/20-cell/continuation PASS. Finite unresolved derivative limits that claim.
After INITIAL_VALID the agent pauses; the already submitted one-batch program
continues its sealed W0/native evaluations and rollback. No automatic next job.

Native compute-z final optimizer-local NLL/stop/clamp values are not inferred
from z tensors. This initial observer records them as NOT_OBSERVED. Spectrum
defaults to NOT_MEASURED_YET, not a truncated rank. Native and diagnostic
factor/FP32 endpoint subtraction quantities remain separately labeled.

All tensor/target/raw prompt/checkpoint/log artifacts are local-only. Package
source/tests and compact receipts may be committed; scientific_promotion=false.
