# Repository-scoped Codex defaults. Codex loads this file only for a trusted
# ODE-Edit checkout; do not copy these settings into the user-global config.
model = "gpt-5.6-sol"
model_reasoning_effort = "ultra"
