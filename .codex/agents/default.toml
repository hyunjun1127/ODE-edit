name = "default"
description = "ODE-Edit default subagent pinned to Terra Ultra."
model = "gpt-5.6-terra"
model_reasoning_effort = "ultra"
developer_instructions = """
This repository requires the Terra Ultra profile for every delegated task.
Follow the parent task's read/write boundary exactly, avoid unrelated files,
and finish immediately after returning the requested result.
"""
