# GH dispatch binding review — REFIT4 write-refresh
2026-09-14. User task GH-REFIT4-WRITE-REFRESH-SEQ1000-20260914-V1.
GH fully read the attachment, final design208 lines, cells61 lines, contract328 lines, prior global review105 lines, design checks31 lines. Read actual pinned BLUE compute_z.py (target hook, loss layer, KL argument order, nonsquared regularizer, Adam/break/clamp loop).
No model/GPU rerun or duplicated SH raw audit was performed. Main staging starts at7e67befa; shared user's untracked originals remain untouched.

Six user-supplied source documents are preserved with exact SHA. CSV CRLF restored mechanically after apply_patch copy and rehashed. Git GH instruction has same text as attachment ignoring line endings/trailing whitespace, not same byte hash; both identities appear in envelope.
Reference full path compatibility, not arbitrary previous trajectory parity, decides reuse40/50/60 new batches. Existing native fitter remains reference, new stepper only in approved namespace. No frozen target reuse across future policy states.
I2/I4 carry rules and fixed anchors/teachers/clamp/counters differ deliberately from REFIT4 fresh reset. Native solver/materialization/history semantics remain fixed.
User latest attachment explicitly demands completion beyond initial/single-batch response. Envelope records this task-only monitoring recall override; other paused tasks unchanged. Middle all paths complete before GH conditional Late candidate decision, no automatic scope expansion.
User cap2 both GPUs used concurrently via independent1GPU jobs and queue, not two processes on one GPU. Hour cap unassigned. Actual source/runtime/environment and resource estimates are SH-owned future locks, not yet claimed.
Global scientific claim author remains GH. Numeric quality/cost references are not AND gates. No result or novelty claim is predetermined.
No dispatch completion, actual GPU gate, submission, or source parity is claimed by this review. These are delegated SH evidence.
