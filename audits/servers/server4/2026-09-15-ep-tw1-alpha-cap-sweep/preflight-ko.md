# SH4 cap-only preflight

원문6개·새 envelope/dispatch/arm locks의 identity와 FULL_READ는 local full-read-receipt에 결속했다. 이전 동일 PROTOCOL/native/reference full-read는 exact identity로 재사용. Shared root와 기존 실행 source/teacher/CAKE 자산은 보존했다.

과학 변경은 NumericalPolicy의 bounded/disabled-null 및 min 제거 분기뿐이다. Legacy bounded1/10/100 CPU fixture12조건에서 alpha/C/네candidate hash exact, RNG 불변. Alpha finite, ball/trust, exact strict-ID quality screen과 minD tie 의미 유지. Native/fitter/model_adapter/ledger/native_map bytes 변경0. Single-arm dispatch validator는 3chain aggregate를 거부한다. User-directed skip callback mock은 진단을 호출하지 않으며 NOT_ESTABLISHED를 유지한다. 기존 source 대비 나머지 추가는 cap scalar receipt와 새task/출력/완주 운영 binding이다.

CAP1 재사용 승인 범위의 기본 경로를 선택했다. 별도 공통 과학 동작 변경이 없으므로 CAP1 재실행0. 기존 checkpoint10의 현재 부재와 남은native/route로selectedW만 복원검증된 경계 유지. 모델-level FD/VJP/ULP/selfKL 재검증0, CPU consistency를 그러한 PASS로 대체0.

Source8c64366c2314f188e034e5f4403fe89f0eaad873의 runtime을 source-v1 archive에 봉인했다. 과거 non-EP import closure는 old frozen bytes를 재사용했고 current-main mutable helper import로 치환하지 않았다. 새 arm별 lock은 동일model/seed/sample/teacher/P/context/dtype/kernel/evaluator와 exact source/metadata/output를 결속한다. 큰 assets는 과거 fullSHA+현재dev/inode/mtime/size, 작은asset와source는 fullSHA로 확인했다. Teacher192 재생성0.

Admission resource-only 조회에서 CAKE48101 예약1, 신규1lane를 사용하여 합산cap2. 48148/48149/48150 모두 upfront held→inspect→release. afterany는 한 chain 실패 시 독립 후속 arm을 품질조건으로 차단하지 않는다. 기존job 변경0, cap늘림0. 명시mem60416M/8CPU/1GPU/12h/exportNONE/Requeue0. 저장은 신규10CP/arm 원schedule 유지, W10L4/M/RNG/ledger 보존; available245889884160B에서60GiB+20GiB 계획(배타reserve아님). 원자료삭제0.

좁은cap/routing11 CPUchecks PASS와 제출source syntax/diff/memory 검사를 수행했다. 그 이후 작성한 분석 경로는 실행archive와 격리된다. CAP1의 기존 저장JSON을 통한 reducer regression은 기존metrics-v3의 모든 CSV/JSON과 byte-identical이었다. 이 회귀검사는 신규arm의 결과나 model numerical validation이 아니다.

미완료: 신규3chain은 마지막 관측PENDING/commit0. 아직 terminal/raw/state/metrics 검증을 했다고 주장하지 않는다. 본task만 명시완주override로 계속하며, CAKE/타task모니터링은 재개하지 않는다. GH FULL_READ/M0와 submission ACK는 각각 local GH-M0-delivery.json/GH-submission-delivery.json에 별도 결속되어 있다. GH 중복검산 요청0.

복잡한 source/analysis 독립 점검을 위한 bounded red agent 호출1회는 agent thread limit으로 시작되지 않았다. 해당 결과를 independent red PASS로 기록하지 않으며 현재 검토는 SH4 parent의 scoped CPU/source review다. 생략된 GPU numerical gate를 이 이유로 복원하지 않는다.
