# P4 Euler projected semantic ODE — server4 E0 audit

## 판정

- instruction: `ODEEDIT-S05-P4-EULER-PROJECTED-SEMANTIC-ODE-V1`
- owner/session: `server4-server-head` /
  `codex://threads/01a028a7-9e3c-7541-81ba-efb40555d17d`
- authoritative contract: `FULL_READ_PASS`
- E0 formula/control implementation: `PASS`
- §17 focused suite: `17/17 PASS`
- no-model dry-plan: `E0_PRE_CALIBRATION_PASS_GPU_HOLD`
- `T_z`: `UNSET`, fail-close
- model load / GPU action / Slurm submit: `0 / 0 / 0`
- next gate: `T_Z_CALIBRATION_PLAN GH APPROVAL`

## Authoritative contract gate

`/data/janghj/ODE-edit/local/state/p4-euler-target-side-semantic-barrier-v1/authoritative-contract.txt`
전체를 EOF까지 읽고 독립 확인했다.

- type: regular non-symlink
- mode: `0600`
- bytes: `21620`
- POSIX newline count: `989`
- SHA256:
  `1899bbfa3610ee2edc94044548475dbb39353ebb7aeafeea371c9a879e7d4398`
- envelope/addendum conflict: 없음

파일은 마지막 newline 없이 마지막 고정 method 문장까지 존재한다. 따라서
`wc -l=989`, logical final record까지 full read이며 truncation 증거가 아니다.

## Adam prior와 새 Euler 경계

기존 P4 Adam M5/M1 source, jobs, reports, result namespace와 main commit
`10b62ed04d9f4f068931a8166d4317ab2597d000`은 immutable prior diagnostic으로
유지했다. 기존 파일을 수정·삭제·재명명하지 않았다.

새 integrator는 old `p4_fixed_target_solver`와
`p4_target_solver_binding`을 import/call하지 않는다. microstep마다 다음 한 번만
사용한다.

`gradient = torch.autograd.grad(per_request_objective.sum(), target_state,
create_graph=False, retain_graph=False)`

그리고 raw field `F=-gradient`를 normalization 없이 사용해:

`z_(m+1)=Projection_B(origin,r)[z_m+(T_z/M)F(z_m;W_k)]`

을 정확히 M회 실행한다. optimizer/Adam/SGD/moment/bias correction,
parameter `.grad`, `loss.backward`, early stop, best selection, accept/reject,
retry/backtracking/fallback는 전부 0이다. `T_z=None`이면 실행 전에 거절한다.

## 구현 구조

| module | 책임 |
| --- | --- |
| `p4_euler_integrator.py` | model/writer/evaluator/optimizer를 모르는 순수 raw projected Euler loop, fixed M, final-only, W content freeze, microstep clamp/field telemetry |
| `p4_euler_binding.py` | 기존 differentiable V+/V± semantic potential과 KL/decay value를 합성하는 얇은 callback; gradient 호출 권한 없음 |
| `p4_euler_cache_transaction.py` | K1–K7 exact C0 restore, K8 append1, abort W/cache rollback, K-specific target bridge SHA |
| `p4_euler_orchestration.py` | fresh current-W outer refresh와 `1/(K-k)` receding-horizon write gain; fixed-endpoint claim 0 |
| `p4_euler_native_reference.py` | EasyEdit canonical MEMIT/AlphaEdit one-shot source boundary; K8/waypoint/provided-z override 0 |
| `p4_euler_runtime_contracts.py` | FP32 tensor-state/solver inventory와 §18 compute ledger |
| E0 numerical lock | `T_z=null`, Stage 1/2 boxed calibration rule, GPU/Slurm false |
| E0 dry-plan | contract/session/source/seal/stream/Native source를 no-model로 결속 |

기존 `p4_semantic_barrier.py`의 context-wise softplus-before-mean 수식만
재사용했다. receipt wrapper는 새 Euler instruction을 별도로 기록하며, Adam
solver나 state는 재사용하지 않는다.

## Formula 및 telemetry gate

Integrator receipt에는 각 microstep의 다음 항목을 포함한다.

- objective, target before/after SHA
- raw field norm, raw Euler displacement
- pre/post clamp displacement, clamp hit/ratio, removed norm/energy
- boundary outward/tangential field component
- zero-field request와 fixed step execution
- logical field 및 actual autograd.grad count

통합 runtime schema는 new/old suffix token, semantic/KL forward,
writer/layer-solve/key-recapture, wall/GPU time, peak GPU memory, cache append를
기록하도록 분리했다. ZA는 W0에서 한 local solve, writer0, K8 repeat0으로
봉인한다. ZB Euler arms만 current W에서 K8 refresh를 수행하고 Native
MEMIT/AlphaEdit는 canonical one-shot external composite reference다.

## §17 focused suite

`project.run_scripts.ode_bf.tests.test_p4_euler_e0`의 정확히 17 gate가 PASS했다.

1. one-step projected Euler exact
2. manual M-step byte/allclose
3. zero field fixed M 전부 실행/update0
4. inner W content hash freeze와 mutation negative
5. Adam/optimizer/backward/state 0, old solver import0, autograd.grad source 1곳
6. Z+/Z± nonsemantic identity
7. context별 softplus 후 mean
8. old field sign `-sigma grad s-`
9. fixed origin/radius clamp idempotence
10. same-`T_z` M5/M10 및 boxed calibration order
11. final iterate only
12. K1–K7 cache restore, K8 append1, abort W/cache rollback
13. K-specific provided/consumed target SHA
14. Euler native compute-z call0
15. canonical EasyEdit MEMIT/AlphaEdit source paths
16. Native one-shot, K8/waypoint/override0
17. full FP32 role inventory와 BF16 negative

`py_compile`도 PASS했다.

## Source/session/dry-plan identity

- source parent:
  `10b62ed04d9f4f068931a8166d4317ab2597d000`
- E0 numerical-lock root:
  `f0393f460386d10c2e6f6dba7d21e077cf1ab19bed4b67f914cb4ab6aa52e61f`
- source-manifest root:
  `26ff684913c24abea72d032bd7d3904af396603178e958a82b5f2bd3051263ed`
- source-manifest SHA256:
  `7b18b96818047b7c589ecf19d163e31c8786351342ea93e105c15f98254253bb`
- dry-plan receipt identity:
  `50098528bc0012ea97b7139d20f792eb3952f3d91be3a9a6800f23101d0083d1`
- EasyEdit runtime root: `/data/janghj/EasyEdit`
- EasyEdit tracked seal SHA:
  `176018298d691a1b67ae7ca787c7dcd2bd77d8606044790833d361fd451ddeb3`
- HF consumed-closure tracked seal SHA:
  `02db39b636625a513aca0d185d3dab04de2b6ba3ece7d867d241dd3a21c28854`
- stream/evaluator/order identities: prior PASS exact match, duplicate full rehash 0
- model load / GPU / submit / heldout: `0 / 0 / 0 / 0`

Native canonical source identities도 dry-plan에서 read-only 봉인했다.

- AlphaEdit main / compute-z:
  `a3a459ab...58b1` / `e12140c6...2c95`
- MEMIT main / compute-z:
  `32f27516...6d0a` / `6e43c1f0...7ae4`

## T_Z_CALIBRATION_PLAN

상세 plan은
`plans/updates/server4/2026-08-22-p4-euler-tz-calibration-plan.md`에 기록했다.

- Stage 1: 공통 fixed `h={0.0625,0.25,1,4}`에서
  `M={1,3,5,10}` strength/pseudo-time curve
- Stage 1 선택: 양 모델·양 arm finite/W-freeze/Adam0, clamp fraction `<0.5`,
  displacement curve coherence를 만족하는 가장 큰 `h`; 다음 후보가 unsafe여야
  closed bracket 성립
- `T_z=5*h_selected`
- Stage 2: 고정 `T_z`에서 M5 대 M10 refinement
- endpoint relative discrepancy HOLD threshold: median `0.10`, p90 `0.25`,
  max `0.50`
- heldout/Native/final performance/A±−A+ 선택 영향 0
- 양 모델 공통 `h`/rule/`T_z`; model-specific branch 0

Calibration unit은 B1 case01 train-only로 제안했고 confirmatory denominator에서
제외한다. 이 sample partition과 수치 plan이 GH 승인되기 전에는 calibration
model load/GPU/Slurm submit을 수행하지 않는다.

## 현재 판정

`E0_PRE_CALIBRATION_PASS_GPU_HOLD`다. Formula/control-plane 구현은 다음 단계에
사용할 수 있으나 `T_z`가 아직 null이고 calibration plan 승인도 없으므로
`PRE_GPU`, `LAUNCHER_READY`, `ZA_SUBMITTED`로 해석하면 안 된다.
