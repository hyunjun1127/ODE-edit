# Terminal owner audit

114 focused CPU tests PASS; two scoped Slurm memory launchers PASS. Git diff/access/raw-free
checks apply to this package only. This is owner audit plus independently implemented CPU
NLL/transition reducer, not an independent red agent/model PASS.

Format exception: default `git diff --check` flagged CR line endings in the newly generated
cell-execution-ledger.csv (Python csv writer convention). Rows were not numerically changed
to satisfy whitespace checking. `git -c core.whitespace=cr-at-eol diff --check` passes for the
complete74-file own-scope source/report delta; other whitespace checks remain active.
All74 paths were checked against the explicit owner write allowlist; forbidden raw extensions0.

Postrun reducer re-read837200 NLL observations and130000 final pairs. RS/PS/NS signs,
at-write→final counts and published CSV agree. Report28members full rehash and separate
CPU reconstruction are byte-identical, including Korean report and4 PNG+4 PDF. The report
manifest's wall clock differs across reproductions by design; member contents do not.
Plots were produced by Python code and visually inspected; no image generation tool.

All original failed/completed evidence remains local, and original12CP/model stat identities
match the current full-hash receipts. No source/raw deletion, raw broadcast, shared environment
mutation, other task restart, new model checkpoint, new edit or z fitting.

C01 failure is not waived: NS row NLL1.6117095947265625e-4 and4 M1 Gram elements violate
their respective locked tolerances. Actual delta/response/solve PASS is a subcheck only.
H1 is supported descriptively for this stream; H2–H4 unresolved. No claim of all31 scientific
cells succeeding. Source publication does not promote these results.

## CPU reproduction (new empty output only)

Use `/mnt/raid5/janghj/EasyEdit/.venv/bin/python`, original isolated Transformers overlay,
and the published package. Original shell argv was not retained in the CPU stage receipts;
the following equivalent commands bind their exact input source map/contract/inventory.
They are instructions, not extra runs performed by this audit.

```bash
python -B -m project.run_scripts.checkpoint_mechanism_audit.archival \
  --source-map /mnt/raid5/janghj/ODE-edit/local/checkpoint-mechanism-audit/20260920-v1/attempt-v1/inputs/source-map.json \
  --contract plans/global/2026-09-20-server2-checkpoint-mechanism-audit-contract-v1.json \
  --output <NEW_EMPTY_ARCHIVAL_OUTPUT>
python -B -m project.run_scripts.checkpoint_mechanism_audit.geometry \
  --contract plans/global/2026-09-20-server2-checkpoint-mechanism-audit-contract-v1.json \
  --source-map /mnt/raid5/janghj/ODE-edit/local/checkpoint-mechanism-audit/20260920-v1/attempt-v1/inputs/source-map.json \
  --checkpoint-inventory audits/global/2026-09-20-server2-geometry-gss-history-review/server2-checkpoint-inventory.json \
  --threads 8 --output-dir <NEW_EMPTY_GEOMETRY_OUTPUT>
```

Exact GPU source/argv/locks are retained in execution-gate-r1 and execution-gate-tech-r1.
No additional GPU reproduction is authorized by this report. Cell timings not separately
instrumented are NOT_RECORDED; shared269 GPU-sec are not charged once per cell.
