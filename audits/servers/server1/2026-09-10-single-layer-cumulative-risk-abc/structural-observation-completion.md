# Structural observation completion (no scientific update change)

The initial Middle sources recorded global operator estimates and global C0
risk, plus local Frobenius/history/L2 terms. The design also requests local
operator/C0 observations and current-key action. The later source adds those
algebraic observations, using the already captured native K, without another
key capture, model forward, optimization, or change to the direct objective.

Before Early native preparation, the admitted process reads all saved Middle
native/scaled and B/C evaluated weights and publishes a distinct correction
receipt. It also applies the previously documented native Torch FP32 C0
division correction. Both original result directories remain immutable.
This is 37 recorded/reproducible weight states, not 37 new edit endpoints.
All compute residency is charged to that admitted job; no extra GPU is used.

`current_key_action = ||(W-We)K||_F^2` is diagnostic only. It is not silently
added to `J_native = tr((W-We)(M+I)(W-We)^T)`. Operator estimates retain the
two fixed seeds, 30 iterations, and residuals and are not upper bounds.
