# Selective inputs for SH4 low-cost write/donor pilot

Instruction: ODEEDIT-S06-LOW-COST-WRITE-DONOR-PILOT-SH4-V1.
Authority: 2026-09-13 user requests this independent SH4 experiment; necessary non-destructive input staging is delegated by GH within that task, not a new general transfer authority.

SH4 may resolve exact immutable files from the 183-row S2 preservation map and completed S1/S4 source/provenance inventories. Required scope is L4-only Middle W50/M50, contexts/RNG/order/config/target-mode closure, completed reusable native capsule and M8 only if identities match, and small completed evidence needed by the governing design. Do not transfer all checkpoints, pretrained model weights, duplicate P/stats, or unrelated raw experiments. Existing S4 assets take priority.

Destinations are fresh /data/janghj/ODE-edit/local/low-cost-write-donor-pilot/20260913-v1/imports/<source-host>/<attempt>/ directories only. SH4 is the sole destination writer; SH1/SH2 provide exact source path/SHA/size/closure as read-only support without resuming their other tasks. Preserve every source copy. No overwrite, --delete, permissions/credential repair, unrelated source/worker mutation.

Before payload movement SH4 writes a concrete member allowlist (source host/absolute path/SHA/bytes/destination), checks free space and source identity, and validates destination nonexistence. One-shot rsync stages into a new partial directory, independently checks complete bytes/SHA/schema/closure, and seals atomically. Partial transfers are not READY. Record custom namespace/nonoverwrite exception to the general broadcast helper; no whole-directory broadcast. Missing closure is reported, not invented. Return receipt links in the server4 audit; do not move or delete the original copies.

No runtime/GPU authority on S1/S2 is conferred. Live E01 scientific outputs are out of scope. No third-server propagation or return deletion.
