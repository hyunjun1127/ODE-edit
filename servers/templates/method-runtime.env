# Copy to servers/local/method-runtime.env and fill local-only values there.
# Do not store token, credential, or private dataset secret in this file.
EASYEDIT_ROOT=
# Shared EasyEdit runtime compatibility variable; keep this name when the
# installed runtime expects it.
KR_UV_BIN=uv
HF_HOME=
HF_HUB_CACHE=
HF_DATASETS_CACHE=
HF_HUB_OFFLINE=1
TRANSFORMERS_OFFLINE=1
PYTHONPATH=
