# v2 execution binding (not a new proposal)

Authoritative design SHA: `4efe1063ea80684beafa791eb99e20c1aa7f3f61636dabab00f9f3805c32eb1d`.
Attachment SHA: `1dee69d0a203fd1816e24452ca614db79fdaaf63e7ad97eb1191a3ef5fdaa80f`.

The independent package implements We-preservation. W0 is absent from the OS
function and BF controller API. The ABC objective and old Q basis are not used.
Original BLUE native solve, subject lookup, evaluator packing and engineering
receipt primitives remain read-only imports. The native target cache is reused
by labeled request, including B1/B7, whose native joint solve is new.

P* is the complete eigenspace of FP64 sym(Praw) above .5. Storing its smaller
excluded complement is an exact operator representation, not rank reduction.
Routing A is applied by full-dimensional projected Cholesky; no Ub100/V256.
OS stationarity and scalar constraint are measured in whitened coordinates.
The BF joint two-variable active-set solve includes cumulative Z anchor and all
three time-scaled cost terms. Frozen fixes only OS gradient directions.

Outcome-free implementation choices:

- Fact identity: Unicode NFC, whitespace split/join, literal relation_id;
  latest canonical ordinal wins. Raw strings are never changed in source assets.
- B1/B7 use the first 1/7 raw ordinals of Middle's current100, then latest-wins.
- Banks use canonical UTF8 JSON with sorted keys, compact separators and literal
  Unicode. Hash includes seed20260911, entry/prepared identity, kind, fact and
  version ordinal. BaseAudit excludes the full Base512 candidate inventory.
- Generation retains the existing ABC metadata-hash rule, min(20,effective B).
  B100 panels and N observations are reused only at matching physical identity.
- Teacher rows retain FP32 full-vocabulary log-softmax; KL accumulation is FP64.
  Prediction positions are last prompt token and following teacher-forced prefix
  tokens. Model/evaluator/tokenizer remain FULL-FP32/eager/offline/right-padding.
- Functional observations keep all original live model parameters unchanged.
  Endpoint evaluation temporarily materializes then restores exact We bytes and
  pointer. History/active-ledger finalization occurs once per new endpoint only.

The 8h Slurm wall limit is a first-run technical reservation, not a scientific
GPU-hour cap. Total hours remain unassigned. Admission cap2, 1GPU/process,
mem182272M, CPU8. Actual full P*/teacher/factor/gradient/evaluation timings at
Middle determine the remaining estimate. No old 8h/48h campaign budget inherits.

CPU fixtures are compact algebra/packing/gradient/transaction checks, not method
performance gates. FP32 central FD fixture uses neighboring binary epsilon values
2^-6/2^-7/2^-8; no production FD controller or efficacy-tuned parameter exists.
All scientific outcomes are retained; scientific_promotion=false.
