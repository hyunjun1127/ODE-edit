# server4 ACK — 2026-08-22

## 최신 지시 확인

- from: GH (`01a0278e-3f20-7b12-8c26-cf71deb2708b`)
- to: `server4-server-head` / `server4`
- status: `done_with_warning`
- direct command: `[GH→SH4 SESSION REGISTRATION]`
- registered SH session:
  `codex://threads/01a028a7-9e3c-7541-81ba-efb40555d17d`
- 지시 내용: onboarding/repo sync/resource audit 완료,
  `PROJECT_GPU_CAP=2` 유지, 신규 experiment job 제출 금지, compact ACK 후
  GH task 명령 대기
- 최신 repo GH 공지:
  `messages/head/2026-08-22-session-registry-reconciliation.md`

## 수행 및 확인

- `main`을 `origin/main`에 rebase 방식으로 동기화했으며 conflict가 없다.
- Git/agent identity를 `server4-server-head`, `server-head`, `server4`로
  설정했다.
- current session/CWD/repository hard boundary checker가 PASS했다.
- `PROTOCOL.md`, `README.md`, `messages/README.md`를 전체 확인했다.
- `tasks/pending/`은 0건이며 server4 대상 `global_head_command`도 0건이다.
  canonical task ID가 없으므로 orphan `tasks/status` 파일은 생성하지 않았다.
- Slurm/resource/storage 점검과 heartbeat를 server4 소유 기록에 반영했다.
- 신규 job, cancel, retry, artifact transfer는 모두 0건이다.

## Task closure 정책 ACK

- broadcast/global-head command는 worker claim 또는 server-head 이동 대상이
  아니다.
- server4는 대상 command별
  `tasks/status/<task_id>/server4.json`에 상태를 기록한다.
- server-head의 `done` 보고만으로 command는 닫히지 않는다. global-head가
  모든 target의 `done` 또는 명시적 `waived`를 확인한 뒤에만 pending task를
  닫고 `tasks/done/<task_id>.closure.md`를 작성한다.

## 자원 요약

- ODE-Edit active project GPU: 0, cap: 2
- Slurm node: `MIXED`, 물리 GPU 8개 중 전체 Slurm alloc 2개
- cap helper: 1 GPU / 65,984 MiB 가상 요청 기준 PASS
- storage: `/data` 사용률 98%, 약 193 GiB 가용 — `WARN`
- 현재 submit 상태: `HOLD`; GH의 신규 task/envelope를 대기한다.

## 근거 경로

- `servers/active/server4.md`
- `agents/server4/server4-server-head.json`
- `audits/servers/server4/2026-08-22-onboarding.md`
- `messages/server-heads/server4/2026-08-22.md`

## S4-M0 storage/execution readiness ACK

- command: `[GH→SH4 TASK S4-M0 — STORAGE/EXECUTION READINESS AUDIT]`
- audit task status: `done`
- storage verdict: `STORAGE_SCOPE_OK`
- available: 206,896,500,736 bytes(약 193 GiB)
- bounded ODE-Edit/EasyEdit/HF total: 103,290,650,624 bytes
- Slurm 2 GPU / 131,968 MiB cap check: PASS
- mutation: delete/move/clean/prune/submit/cancel/retry/transfer 모두 0건
- addendum: Llama/Qwen projector와 stats canonical SHA exact MATCH,
  `PROTECTED_REUSABLE=22,575,859,404 bytes`; 두 모델 asset은
  `READY_REUSE`. current legacy lock의 `/mnt/raid5/...` root는 server4에 없어
  launcher는 server4-specific path seal 전 `BLOCKED`다.
- detail: `audits/servers/server4/S4-M0-storage-execution-readiness.md`
- compact report:
  `messages/server-heads/server4/2026-08-22-S4-M0.md`
